## Packages
framer-motion | Smooth animations for hero section and cards
react-icons | Iconic icons for the UI (FaPaw, FaHeart, etc.)
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
Authentication uses the /api/register, /api/login, /api/logout endpoints.
Pet data is currently mocked on the frontend as no API was provided for pets in the prompt schema.
Images use Unsplash source URLs with descriptive keywords.
Font chosen: 'Comic Sans MS' (as requested) + 'Inter' for body.
